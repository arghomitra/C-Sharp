using System;
using System.Drawing;
using System.IO;
using System.Runtime.InteropServices;

namespace split_string
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // string word = "1,2,3,4,5";
            //Console.WriteLine(word.Split(','));    //output : System.String[] 
            /*foreach (var a in word.Split(','))
            {
                Console.WriteLine(a);
            }*/
            /*string words = "This is rewally fun. I am learning to code.";
            char[] splitFunction = new char[] { ' ', '.' };
            foreach (var i in words.Split(splitFunction, StringSplitOptions.RemoveEmptyEntries))
            {
                Console.WriteLine(i);
            }*/

            //string numbers = "1,2.3/4;5'6";
            //string[] nums = split(numbers, ",");


            // Fill a rectangle with myColor.
            // e.Graphics.FillRectangle(new SolidBrush(myColor), 0, 0,
            // 100, 100);
            // Create an integer representation of an OLE color.
            
        }
        
    }
}

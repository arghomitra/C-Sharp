using System;
using System.Linq;

namespace length_practic
{
    internal class Program
    {
        static void Main(string[] args)
        {
           string first =Convert.ToString(Console.ReadLine());
            string second = Convert.ToString(Console.ReadLine());
            int firstString=first.Length;
            int secondString=second.Length;
            string[] count = { first };
            string place = count.ElementAt(0); 
            Console.WriteLine(place);
            /*
             if(firstString > secondString)
            {
                for (int i = 0; i < firstString; i++)
                {
string[] count ={first};

                }
            }*/

            name = argho,asif,sifat
                s
        }
    }
}
